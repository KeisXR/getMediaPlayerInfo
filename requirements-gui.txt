# GUI-specific dependencies for getMediaPlayerInfo Desktop
# Install with: pip install -r requirements-gui.txt

# GUI Framework
PySide6>=6.6.0

# Optional: QR code generation for mobile access (API Server tab)
# Install for QR display: pip install qrcode[pil]
# qrcode[pil]>=7.4.2

# Optional: Last.fm scrobbling
# pylast>=5.2.0

# Base dependencies (already in requirements.txt — repeated here for convenience)
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
websockets>=12.0
aiohttp>=3.9.0
yt-dlp>=2024.0.0
pypresence>=4.3.0

# Windows Runtime bindings (required on Windows for SMTC media detection)
winrt-runtime>=2.0.0; sys_platform == "win32"
winrt-Windows.Foundation>=2.0.0; sys_platform == "win32"
winrt-Windows.Foundation.Collections>=2.0.0; sys_platform == "win32"
winrt-Windows.ApplicationModel>=2.0.0; sys_platform == "win32"
winrt-Windows.Media.Control>=2.0.0; sys_platform == "win32"
winrt-Windows.Storage.Streams>=2.0.0; sys_platform == "win32"
winrt-Windows.UI.Notifications>=2.0.0; sys_platform == "win32"
winrt-Windows.UI.Notifications.Management>=2.0.0; sys_platform == "win32"

# Linux D-Bus bindings (required on Linux for MPRIS media detection)
dbus-python>=1.3.2; sys_platform == "linux"
PyGObject>=3.46.0; sys_platform == "linux"
